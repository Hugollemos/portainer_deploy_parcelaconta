name: 'Action to deploy stacks to portainerr'
description: 'Action to deploy stacks to portainerr'
author: Hugo Lemos da Silva
branding:
  icon: 'bar-chart-2'
  color: 'yellow' 
outputs:
  saida:
    description: "success in deploy"
    value: ''
runs:
  using: "composite"
  steps:
    - run: stack.sh
      shell: bash